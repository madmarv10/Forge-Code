# Secrets — never commit
.env
.env.*
!.env.example

# Build artifacts (keep the runnable bundle.js, exclude the large sourcemap)
dist/*.map
build-errors.txt

# Dependencies / lockfile (not reproducible from package.json — see README)
node_modules/
bun.lock
