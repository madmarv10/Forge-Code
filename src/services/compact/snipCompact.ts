// STUB: snip compact (HISTORY_SNIP feature). Returning false disables snips.
export function isSnipRuntimeEnabled(): boolean {
  return false;
}
export function shouldNudgeForSnips(_messages: unknown): boolean {
  return false;
}
